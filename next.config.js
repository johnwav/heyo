/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["sp-images.summitpost.org", "heyochatbucket.s3.eu-north-1.amazonaws.com"],
  },
};

module.exports = nextConfig;
